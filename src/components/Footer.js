import styles from "@/styles/styleComponents/Footer.module.css";

const Footer = () => {
  return (
    <div className={styles.footer_container}>
      <p>Footer</p>
    </div>
  );
};

export default Footer;
